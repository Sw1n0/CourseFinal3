[
  {
    id: int
    state: str
    date: str
    operation amount: { amout:str , currency: {name: str, code: str}}
    description: str
    from: str can be none
    to: str




функции
0) чтение из файла json и конвертация в список питона
1) фильтр по статусу executed
2) сортировка по времени перевода и оставить 5 операций
3) изменение формата даты
4) маскировка карты
5) маскировка счета
6) вывод

тесты

1) количество элементов со статусом canceled = 0
2) ? создать json, 2 файла
3) берем конкретный пример и выводим желаемый результат на его основе?
4) аналогично?
5) аналогично?
6) ?